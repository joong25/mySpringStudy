DI 유용한 이유
= 객체 생성에 사용할 클래스를 변경할 때 객체를 사용하는 코드를 변경하지
않고 객체를 주입하는 코드 한 곳만 변경하면 된다

그러면 실제 객체 생성하는 코드는 어디에?
메인(Main) 메서드에서 생성하면 된다.

But 
객체를 생성하고 의존 객체를 주입해주는 클래스를 따로 만들면 유지 보수에
더 좋을 것이다.
의존 객체 주입 = 서로 다른 두 객체를 조립한다
-> 조립기 클래스 있으면 좋을 것(Assembler)

정리하면 조립기는 객체를 생성하고 의존 객체를 주입하는 기능을 제공